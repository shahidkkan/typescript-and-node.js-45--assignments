let guest_list : string[] = ["zakir","faheem", "adil", "aftab", "zeeshan"];

console.log(`i am inviting ${guest_list.length} number of friends to my dinner which are following\n`);

for(let i=0; i< guest_list.length;i++);{

    console.log(`${ i + 1 }.${guest_list[i]}`)
}
